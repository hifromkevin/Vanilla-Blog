extends layout
block title
	title #{title}
block content
	h1 #{heading}
	ul.list-group
		each article, i in articles
			li.list-group-item
				a(href="/articles/"+article._id )= article.title 
				p By #{article.author}
				p #{article.timestamp}
				hr